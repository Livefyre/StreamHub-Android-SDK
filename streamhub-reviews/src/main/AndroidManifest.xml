<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="livefyre" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="com.android.launcher.permission.INSTALL_SHORTCUT" />
    <uses-permission android:name="com.android.launcher.permission.UNINSTALL_SHORTCUT" />

    <application
        android:hardwareAccelerated="true"
        android:allowBackup="true"
        android:name="livefyre.LivefyreApplication"
        android:icon="@mipmap/livefyreappicon"
        android:label="@string/app_name"
        android:theme="@style/AppTheme.Base" >
        <activity
            android:name=".activities.SplashActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />

            </intent-filter>
        </activity>
        <activity
        android:screenOrientation="portrait"
        android:name=".activities.ReviewsActivity"
        android:label="@string/app_name"/>
        <activity
            android:screenOrientation="portrait"
            android:name=".activities.ReviewInDetailActivity"
            android:label="@string/app_name"/>
        <activity
            android:screenOrientation="portrait"
            android:name=".activities.NewReviewActivity"
            android:label="@string/app_name"/>
        <activity
            android:screenOrientation="portrait"
            android:name=".activities.EditReview"
            android:label="@string/app_name"/>
        <activity
            android:screenOrientation="portrait"
            android:name=".activities.ReplyReview"
            android:label="@string/app_name"/>
    </application>

</manifest>
